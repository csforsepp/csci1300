#include <iostream>
#define _USE_MATH_DEFINES
#include <math.h>
#include <cmath>
using namespace std;

int convertSeconds (double seconds){{
    int s = seconds / 3600;
    
        cout << hours << " hour(s) " << minutes << " minute(s) " << s << " second(s) " << endl; 
    
}
return 0; 
}

int main (){
    convertSeconds(3671);

}